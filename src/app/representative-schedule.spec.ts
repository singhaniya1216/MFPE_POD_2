import { RepresentativeSchedule } from './representative-schedule';

describe('RepresentativeSchedule', () => {
  it('should create an instance', () => {
    expect(new RepresentativeSchedule()).toBeTruthy();
  });
});
